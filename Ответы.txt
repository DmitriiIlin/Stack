ДЗ №2
while stack.size() > 0:
    print(stack.pop())
    print(stack.pop())

Мы имеем стек 3.14 "2" 1 где 1 голова.
1) В результате первого pop() мы удалим 1, в результате второго "2".
2) 2 итерация цикла начнеться со значения 3.14 , после первого pop мы получим пустой stack. То есть вторая операция pop выдаст None
3) Цикл закончиться.

ДЗ №3 Мера сложности.
push при проведении операции push нам необходимо сдвинуть все элементы списка, поэтому сложность О(n) , если мы реализуем push() с помощью insert()
pop O(1)
 
 